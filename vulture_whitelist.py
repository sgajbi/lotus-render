"""Reviewed Vulture suppressions.

This file is intentionally empty while the dead-code inventory has no false positives. Add only
imported, executable symbol-reference expressions with an adjacent rationale. The unit contract
executes this module so a renamed or removed symbol makes the allowlist fail instead of silently
outliving the code it exempts.
"""
